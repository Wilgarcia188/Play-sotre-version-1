<resources>
    <!-- Pure-Compose app: this theme only drives the window before Compose draws,
         so it just has to match the app background and let the app draw edge to edge. -->
    <style name="Theme.PdfSuite" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
